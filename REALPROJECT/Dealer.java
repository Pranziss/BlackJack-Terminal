package TerecsPlayground.REALPROJECT;
//dealer and user have the same code since they are both specialized version of the player
public class Dealer extends Player {
    public Dealer(String name, int money) {
        super(name, money);
    }
}
