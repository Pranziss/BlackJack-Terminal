package TerecsPlayground.REALPROJECT;
//no need to duplicate them since they both share the same attributes
public class User extends Player {
    public User(String name, int money) {
        super(name, money);
    }
}
